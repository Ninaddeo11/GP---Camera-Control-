# Test-only dependencies — not installed in the production Docker image
# (Dockerfile only ever installs requirements.txt). Install with
# `pip install -r requirements.txt -r requirements-dev.txt` for local test
# runs; see tests/README.md (or the top-level docs/anpr-pipeline.md) for
# what these tests do and don't cover.
pytest==8.3.3
